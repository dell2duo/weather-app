import React from 'react'
import { View } from 'react-native'
import { City } from '../../domain/models/City'

// import { Container } from './styles';

type Props = {
  city: City
}

export const DetailsView: React.FC<Props> = ({ city }) => {
  return <View />
}
