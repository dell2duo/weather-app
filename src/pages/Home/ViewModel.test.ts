import { HomeViewModel } from './ViewModel'

test('should get a city value by search component', () => {
  //arrange
  // const viewModel = new HomeViewModel()
  //act
  // const citys = viewModel.searchCity('London')
  //assert
  // expect(citys).toContain('London')
})
